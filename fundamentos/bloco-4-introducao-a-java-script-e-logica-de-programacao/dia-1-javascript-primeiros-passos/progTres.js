const numUm = 200;
const numDois = 100;
const numTres = 300;

if (numUm > numDois && numUm > numTres){
    console.log(numUm);
} else if (numDois > numUm && numDois > numTres){
    console.log(numDois);    
} else{
    console.log(numTres);
}